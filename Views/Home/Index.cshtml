<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet" />
    <link href="~/css/bootstrap.min.css" rel="stylesheet" />
    <link href="~/css/site.css" rel="stylesheet" />
    <style>
        body {
            font-family: 'Poppins', Arial, sans-serif;
            background: linear-gradient(135deg, #2C6E49, #153447,#294C60);
            background-attachment: fixed;
            color: #333;
            margin-top: 0;
        }

        .navbar {
            background: #002b5c;
            box-shadow: 0px 6px 15px rgba(0, 0, 0, 0.1);
            position: sticky;
            top: 0;
            z-index: 100;
            padding: 1rem;
        }

        .navbar-brand {
            color: #ffffff !important;
            font-weight: 700;
            font-size: 1.8rem;
            letter-spacing: 2px;
        }

        .navbar-nav .nav-link {
            color: #ffffff !important;
            font-weight: 500;
            padding: 12px 20px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

            .navbar-nav .nav-link:hover {
                color: #FFD700 !important;
                background-color: rgba(0, 0, 0, 0.15);
                border-radius: 4px;
            }

        .hero-section {
            background: url('https://via.placeholder.com/1920x800') center center no-repeat;
            background-size: cover;
            color: #ffffff;
            text-align: center;
            padding: 100px 0;
            position: relative;
        }

            .hero-section h1 {
                font-size: 50px;
                font-weight: 700;
                letter-spacing: 3px;
                text-shadow: 3px 3px 5px rgba(0, 0, 0, 0.4);
            }

            .hero-section p {
                font-size: 18px;
                margin-top: 20px;
                font-weight: 500;
            }

        .btn-primary {
            background-color: #f39c12;
            border-color: #f39c12;
            font-weight: 600;
            border-radius: 5px;
            padding: 12px 30px;
            letter-spacing: 1px;
            transition: all 0.3s ease;
        }

            .btn-primary:hover {
                background-color: #e67e22;
                border-color: #e67e22;
                transform: scale(1.05);
            }

        .section {
            margin-bottom: 40px;
            padding: 40px 30px 20px; /* Reduced bottom padding */
            background-color: #f4f7fa;
            border-radius: 12px;
            max-width: 1020px;
            margin-left: auto; /* Centering the section */
            margin-right: auto;
        }

        .section-title {
            font-size: 32px;
            font-weight: 600;
            color: #2f3e4e;
            text-align: left;
            margin-bottom: 25px;
            border-bottom: 2px solid #007bff;
            padding-bottom: 10px;
            letter-spacing: 1px;
        }

        .card {
            border: 1px solid #e2e8f0;
            border-radius: 12px;
            box-shadow: 0px 10px 20px rgba(0, 0, 0, 0.1);
            background-color: #ffffff;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            overflow: hidden;
            margin-bottom: 20px;
        }

            .card:hover {
                transform: translateY(-10px);
                box-shadow: 0px 15px 30px rgba(0, 0, 0, 0.15);
            }

        .card-header {
            background-color: #007bff;
            color: white;
            font-weight: 600;
            font-size: 1.2rem;
            padding: 18px;
            border-top-left-radius: 12px;
            border-top-right-radius: 12px;
            text-transform: uppercase;
        }

        .card-body {
            padding: 30px;
            font-size: 1rem;
            color: #495057;
        }

        footer {
            background-color: #003459;
            color: #ffffff;
            text-align: center;
            padding: 20px 0;
            font-size: 0.875rem;
            position: fixed;
            bottom: 0;
            width: 100%;
        }

            footer p {
                margin-bottom: 0;
            }
    </style>
</head>

<body>
    <div class="container-fluid">
        <main role="main">
            <div class="hero-section">
                <h1>Welcome to Your Task Manager</h1>
                <p>Organize, track, and manage your tasks like never before</p>
                <a href="@Url.Action("Create", "Task")" class="btn btn-primary">Get Started</a>
            </div>

            <div class="section">
                <div class="section-title">Overview</div>
                <div class="row">
                    <div class="col-lg-6 col-md-12 mb-4">
                        <div class="card">
                            <div class="card-header">
                                Task Overview
                            </div>
                            <div class="card-body">
                                <p>Manage tasks efficiently, set deadlines, and track progress with ease.</p>
                                <a href="@Url.Action("Create", "Task")" class="btn btn-primary">Add New Task</a>
                            </div>
                        </div>
                    </div>
                    <div class="col-lg-6 col-md-12 mb-4">
                        <div class="card">
                            <div class="card-header">
                                Employee Overview
                            </div>
                            <div class="card-body">
                                <p>Assign tasks, track employee progress, and ensure project completion.</p>
                                <a href="@Url.Action("Create", "Employee")" class="btn btn-primary">Add New Employee</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </main>
    </div>

    <script src="~/js/jquery.js"></script>
    <script src="~/js/bootstrap.bundle.min.js"></script>
</body>
</html>
